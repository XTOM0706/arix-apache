# 图形化（X11 / VNC）真机验证清单 —— 2026-07-30

代码全部编译通过（debug 33.05MB / release 14.59MB，零警告），**真机一次都没跑过**。
分支 `work/0728-terminal-fixes`，未提交。设计说明见 `TODO-TERMINAL-APP.md` 最后一节。

---

## 一、东西在哪

### 1.1 装什么、从哪进

| 项 | 位置 |
|---|---|
| 要装的 APK | `E:\OnyxAI\terminal\build\outputs\apk\debug\terminal-debug.apk`（release 在 `apk/release/`） |
| 包名 | `com.arix.terminal`（独立 App，和主 App `com.arix.*` 分开装） |
| 界面入口 | 终端里左上角菜单 → 抽屉里新增的 **「图形界面」**（在「文件」和「个性化」之间） |
| 外部 X server | 需另装 `com.termux.x11`（Termux:X11 nightly，GPLv3，**不在我们包里**）。页面上没装时给「去下载」按钮，指向 `https://github.com/termux/termux-x11/releases` |

### 1.2 源码

全部新增在 `terminal/src/main/kotlin/com/arix/terminal/gfx/`：

| 文件 | 行数 | 干什么 |
|---|---|---|
| `GraphicsEnv.kt` | 257 | 地基。socket 目录 / `/dev/shm` / `DISPLAY` / 分辨率 / 后端选择的唯一收口点 |
| `X11Server.kt` | 255 | 探测外部 X server App、`app_process` 直起、降级 flag、拉起显示端 |
| `VncServer.kt` | 514 | 容器内 Xvnc：装 / 起 / 停 / 密码 / 桌面配方 |
| `rfb/RfbClient.kt` | 1010 | 自写 RFB 3.8 客户端 |
| `rfb/Keysyms.kt` | 110 | keysym 映射 |
| `VncCanvasView.kt` | 454 | 画布 + 触摸/键盘 → RFB 事件 |
| `VncViewerActivity.kt` | 272 | 内置查看器界面 |
| `GfxActivity.kt` | 473 | 「图形界面」控制页 |
| `GfxService.kt` | 127 | 前台服务，钉住跑着图形会话的进程 |

改到的既有文件：`TerminalEnv.kt`（四条 argv + `buildEnv` + `sessionSignature`）、
`TerminalNav.kt`（`TermDest.GFX`）、`AndroidManifest.xml`、`NOTICE`。

### 1.3 运行期落在设备哪儿

宿主侧（App 私有目录 `/data/data/com.arix.terminal/files/`，要 root 才看得到）：

| 路径 | 是什么 |
|---|---|
| `files/x11/` | X server 的 `TMPDIR` |
| `files/x11/.X11-unix/X<N>` | **X socket 本体**。整条路通不通就看它出不出现 |
| `files/shm/` | 顶替 `/dev/shm`（1777） |

容器侧（guest 视角）：

| guest 路径 | 绑到 |
|---|---|
| `/tmp/.X11-unix` | `files/x11/.X11-unix`（所有环境） |
| `$PREFIX/tmp/.X11-unix` | 同上（**只有 Termux 环境有这条**，因为 Termux 的 libX11 被上游改到了前缀里） |
| `/dev/shm` | `files/shm` |

配置全在 SharedPreferences `term_ui` 里：`gfx_enabled` / `gfx_backend` / `gfx_x11_display`(默认 0) /
`gfx_vnc_display`(默认 1) / `gfx_width` / `gfx_height`(默认 720×720) / `gfx_dpi`(96) /
`gfx_x11_flags` / `gfx_vnc_password` / `gfx_vnc_desktop`。

### 1.4 两条路各自的启动链

**X11**：
```
/system/bin/app_process / --nice-name=arix-x11 com.termux.x11.CmdEntryPoint :0 [降级flag]
  env: CLASSPATH=<com.termux.x11 的 apk 路径>   TMPDIR=/data/data/com.arix.terminal/files/x11
```
进程是我们 fork 的、uid 是我们的，所以 socket 写得进我们的私有目录。

**VNC**（容器内，Xvnc 是前台进程，靠 proot `--kill-on-exit` 整棵树一起收）：
```
写 ~/.vnc/passwd（vncpasswd -f）→ 清 /tmp 与 $PREFIX/tmp 下的 .X<N>-lock 和旧 socket
→ ( sleep 2; DISPLAY=:1 <桌面命令> ) &
→ exec Xvnc :1 -geometry 720x720 -depth 24 -dpi 96 -rfbport 5901 -localhost
       -AlwaysShared -desktop Arix -SecurityTypes VncAuth -PasswordFile ~/.vnc/passwd -nolisten tcp
```
桌面命令：`xterm` / `openbox-session || { openbox & xterm; }` / `startxfce4` / 不起。

装包（按当前环境的包管理器族分派，`tigervnc` 本体）：
TERMUX 先 `pkg install -y x11-repo` 再 `tigervnc`；DEBIAN `tigervnc-standalone-server`；
ALPINE/ARCH/SUSE/VOID `tigervnc`；FEDORA `tigervnc-server`。

---

## 二、要测什么（按「先做这个，不通后面都白搭」排序）

### T0. 回归：图形关着时终端跟以前一模一样
**这是第一步，不是可选项** —— 图形地基动了全部四条启动路径。
- 装完新 APK，**先什么都别开**，进终端敲 `ls`、`pkg --help`、切一次发行版、跑一条 AI 侧的 `linux_exec`。
- 判据：和上一版没有任何差别。有差别就地停下，问题在 `TerminalEnv` 的绑定，不在图形本身。

### T1. ⭐ X socket 能不能被容器里的客户端连上（整条路的生死判据）
这正是当初计划里「零代码先验」的 Step 0，被跳过了，所以第一次上机就是补它。
1. 装 Termux:X11（`com.termux.x11`）。
2. 图形界面页 → 打开「启用」→ 后端选 X11 → 点「启动」。
3. **看 `files/x11/.X11-unix/X0` 出没出现**（有 root 就 `su -c 'ls -l /data/data/com.arix.terminal/files/x11/.X11-unix/'`；没 root 就看页面上「socket 已就绪」这句）。
4. 回终端页（会自动重开会话），`echo $DISPLAY` 应为 `:0`。
5. Termux 环境：`pkg install x11-repo` → `pkg search xterm` 找到包名装上 → 敲 `xterm`。
6. **判据：Termux:X11 的窗口里出现 xterm，且能打字。**

失败时先分三段定位：`X0` 有没有（server 起没起）→ 容器里 `ls -l /tmp/.X11-unix/` 有没有（绑定通没通）→
`echo $DISPLAY`（会话有没有重开）。⚠️ Termux 环境要额外确认 `$PREFIX/tmp/.X11-unix/` 里也看得到。

### T2. `app_process` 这条启动链本身
- 起不来的话页面「输出」区会有 server 的 stdout/stderr。
- 具体要证伪的三件事：targetSdk 28 的 App 能不能 exec `app_process`；
  入口类名 `com.termux.x11.CmdEntryPoint` 是否还对得上当前 nightly；
  `CLASSPATH` 取的 `sourceDir`（含 split）对不对。
- 「显示端」按钮拉起的是 `com.termux.x11.MainActivity`，对方改过类名就会走 launcher intent 兜底。

### T3. ⭐ Android 12+ 幻影进程上限
X11/VNC 共同的最高风险，前台服务挡不住它。
- 现象：桌面跑着跑着画面**突然断开**，logcat 里有 `signal 9` / phantom process 相关。
- 怎么看：`adb logcat | grep -i phantom`，或 `dumpsys activity processes | grep -i phantom`。
- VNC 的进程树最长（Xvnc + WM + 终端 + 应用），最先中枪 —— 所以 VNC 那档先用 `xterm` 别上 xfce4。

### T4. Xvnc 起不起得来
1. 后端切 VNC → 桌面配方选 **xterm**（别一上来 xfce4）→ 点「安装 Xvnc 与桌面」，看输出面板。
2. 装完点「启动」，看输出里 Xvnc 的日志。
3. 具体要证伪的：
   - Termux 版 tigervnc 能否在**不带 `-0`** 的 proot（app uid）里起来；
   - `-dpi` / `-nolisten tcp` / `-AlwaysShared` 三个参数是否被实际打包的版本接受（任一不认，Xvnc 直接退出）；
   - `vncpasswd -f` 在不在、输出格式是否被 `-PasswordFile` 认；
   - Termux 的 xterm 包名对不对（代码里写的是 `xterm`，可能要改）。
4. **停止后要复查两件事**：容器里没有残留的 `openbox/xfce/xterm` 进程；**再点一次启动能起来**
   （不能起 = `/tmp/.X1-lock` 残留清得不对，脚本里清的是 `/tmp` 和 `$PREFIX/tmp` 两处，这两处猜得对不对没验过）。

### T5. ⭐ 自写 RFB 客户端（三处高危，症状各不相同）
点「打开桌面」进内置查看器。
- **认证失败** → VncAuth 的 DES 位逆序，或 Android 的 provider 不给 `DES/ECB/NoPadding`。
  隔离办法：临时改成无密码（`-SecurityTypes None`）先把 None 那条走通。
- **整片颜色不对 / 红蓝互换** → 服务端没照我们的 `SetPixelFormat` 走，或 CPIXEL 的字节序判反了。
  这个**不会报错**，最容易被误判成解码写错。第一眼就看颜色。
- **花屏 / 几秒后报「ZRLE 不同步」** → ZRLE 解码。子 agent 在「每个矩形解完必须刚好用完输入」处
  故意加了严格自检，报这个就说明衔接有问题（或自检判太死）。三个嫌疑点：跨矩形共用 Inflater 的
  输入衔接、调色板位打包的行末字节补齐、CPIXEL 3 字节。
- 另外两条只有真操作才走到：拖窗口/滚动（CopyRect 重叠区的行序）、中途改分辨率（DesktopSize）。

### T6. 输入与手感（小屏/圆屏重点）
- 直接触摸模式：点按钮、拖窗口。
- 触控板模式：**圆屏上能不能点中菜单项**（这是它存在的唯一理由）。虚拟指针看不看得见、
  双指轻点出不出右键、双指拖能不能滚。
- 键盘：顶栏键盘钮 → 软键盘出不出；中文输入法走的是 `commitText` 那条，英文/回车走 `sendKeyEvent`。
- 扩展键条：Esc / Tab / 方向键；Ctrl 粘滞一下再按 c，看远端收没收到 Ctrl+C。
- 缩放平移：双指捏合以捏合点为锚、「贴合屏幕」按钮。

### T7. 前台服务与生命周期
- 启动任一后端后，通知栏出现「图形会话运行中」，点「全部停止」两条都停、通知消失。
- 把终端 App 切后台 / 熄屏一会儿再回来，桌面还在。
- 查看器页面按返回退出，不应该把 Xvnc 一起带走（只断连接）。

### T8. 设置联动
- 改「启用」开关、改后端、改显示号 → **回终端页应自动重开会话**（这三项进了 `sessionSignature`）。
  改完 `echo $DISPLAY` 要跟着变。
- 改分辨率**不会**重开会话（它只影响服务端），要重启 Xvnc / X server 才生效。
- X11 的四个降级 flag（`-legacy-drawing` / `-force-bgra` / `-disable-dri3` / `-force-sysvshm`）
  改完要重启 X server；黑屏花屏时一条条试。

---

## 三、明确没做，别当 bug 报

音频（`PULSE_SERVER` 整条链路一次都没查证）、GPU 加速（virgl / Turnip / Zink，且都是 bionic
二进制，glibc rootfs 用不了）、Wayland、我们自己 fork 的 `com.arix.x11` APK（那是单独一个仓 + NDK 构建）。

系统终端（Android shell）里没有图形 —— 它不进容器，没有 X 客户端也没地方放 socket。

---

## 四、附加：给 AI 的环境简报（2026-07-30 同批加的）

**东西在哪**：`terminal/.../TerminalBrief.kt`（英文简报，运行时现算）→ AIDL 新方法 `aiBrief()`
（`app/` 与 `terminal/` **两份 .aidl 都改了，必须一致**）→ `TerminalClient.aiBrief()` →
`LocalLinuxTool` 的新 `action=env`，以及**每个进程第一条 run/start 自动带一次**。

**要测什么**

- T9-1 在 Termux 环境让 AI 跑一条命令（比如 `ls`）→ 结果开头应多出一段英文简报，
  里面写着 Termux / `pkg install -y` / 共享目录 / graphics off。
- T9-2 切到某个已装发行版（如 Alpine）→ 让 AI 再 `action=env` → 应报 Alpine / `apk add`，
  而不是 `apt`。**这是这个功能存在的理由**：切了环境 AI 还敲 apt 就是没生效。
- T9-3 打开图形化 → `action=env` → 应报 `DISPLAY=:0`（或 `:1`）、分辨率、后端跑没跑。
- T9-4 简报只在一个进程里出现一次（第二条命令的结果里不该再有），但显式 `action=env` 每次都给。
- T9-5 装着**旧版**终端 App 时新主 App 不能崩：`aiBrief()` 在旧 APK 上没有这个 AIDL 方法，
  调用会抛，代码里是 `runCatching` 吞掉当作「这次不给简报」—— 命令本身必须照跑不误。

---

## 五、让 AI 自己把图形化拉起来（同日追加）

**为什么要单独做**：图形化的三样东西全在**容器外面** —— 总开关是终端 App 的 prefs、
X server 是它 fork 的 `app_process` 子进程、Xvnc 是它托管的 Process。AI 只能在容器里敲命令，
够不着任何一样。没有这条，它能装上 xterm，然后卡在 `cannot open display`，
接着去装更多包试图修一个根本不在容器里的问题。

**东西在哪**：`terminal/.../gfx/GfxControl.kt` → AIDL `gfxControl(String op)`（两份 .aidl 都改）
→ `TerminalClient.gfxControl()` → `linux_exec` 的 `action=gfx`，操作词放在 `command` 里
（`on` / `off` / `start` / `stop` / `view` / `status`；复用 `command` 是为了不给每轮的 schema 多加字段）。
`ToolPermission` 里每个操作都有自己的一句话，审批弹窗上写得清清楚楚。

**故意不给 AI 的**：改分辨率、换后端、改降级 flag、装 X server App —— 前者是用户的审美选择，
后者需要用户在系统层面点同意。

**AI 应该走的完整流程**（也是要测的路径）：
1. `action=env` → 看到 graphics off
2. `action=gfx, command=on` → 弹审批 → 打开。**AI 的下一条命令就已经有 DISPLAY**
   （它每条命令都是新起一个 proot，环境当场算；只有用户那条长命的交互 shell 才需要重开会话）
3. 普通 `run` 装 X 客户端（`pkg install x11-repo` / `apk add xterm` …）；VNC 还要装 tigervnc
4. `action=gfx, command=start`
5. `run` 里 `xterm &`（**必须后台**，否则这条命令永远不返回）
6. `action=gfx, command=view` 把桌面推到用户屏幕上

**要测什么**
- T10-1 上面六步整条走通，AI 全程没去装无关的包。
- T10-2 `command=on` 之后，AI 下一条 `echo $DISPLAY` 就有值（**不需要任何额外动作**）。
- T10-3 `command=off` 会把正在跑的 server 一起停掉（不然留个没人看得见、还在占进程数的 X server，
  而进程数正是幻影进程上限盯着的）。
- T10-4 每个操作都弹审批、文案对得上（打开支持 / 关掉并停服务 / 启动 / 停止 / 显示到屏幕）。
- T10-5 `command=view` 会抢占屏幕 —— 确认它没被塞进 start 里顺手做掉。
- T10-6 X11 后端且没装 X server App 时，`status` 要明说「只有用户能装」，AI 不该反复重试 start。
