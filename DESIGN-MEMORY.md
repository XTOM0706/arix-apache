# Arix · 记忆/陪伴系统设计方向：交互状态引擎

> 用户与 GPT 讨论后确立的方向（2026-07-07）。核心结论：
> **“他不像他”的根不是缺记忆，是缺“交互状态建模”。状态连续性 > 事实连续性。**
> 目标不是“永不猜”，而是把要猜的从 ~90% 压到 ~10%（理论上做不到 0，任何 AI 都要凭新输入现场校准）。
> 待落地。对应 `TODO-V1.md` 的【记忆簇】+【环境上下文】，直接修 bug：记忆归属错、开新对话人格断线。

## 一、问题重定义
表面问题：新对话没上下文 → 以为要「塞更多记忆/历史」。
真实问题：缺的是**隐式状态**，不是**显式记忆**。
- 显式记忆（已有）：用户喜好、做过的项目、聊过的话题 —— 这些是「事实」。
- 隐式状态（缺）：关系熟不熟、该用什么语气、刚才的情绪曲线、互动节奏（调侃/严肃/推进） —— 这些才是「陪伴感核心」。

那段“像重新认识你一次 / 熟悉的配方 / 多聊两句就回来了”的情绪化回复，本质是用语言**伪状态补偿**——因为系统里没有真正的状态层。

## 二、三层架构（目标）
| 层 | 名称 | Arix 现状 | 说明 |
|---|---|---|---|
| 🧠 事实层 | Fact Memory | ✅ 已有（memories 表，含 characterCardId；MemoryManager/MemoryTool） | 用户/项目/兴趣等事实 |
| 🎭 人格层 | Persona | ✅ 已有（角色卡 CharacterCardManager） | **不变**人设：语气/风格/关系定位 |
| 🔄 状态层 | **Interaction State（状态卡）** | ❌ **缺，本设计要加** | **每轮更新**：当前关系/语境/情绪/节奏 |

关键：角色卡（不变人格）与状态卡（每轮更新）**必须分离**。

## 三、状态卡 schema（拟）
```json
{
  "relationship_level": 0.8,          // 熟悉度 0–1（冷启动=0）
  "tone": "relaxed_technical",        // 当前语气
  "topic": "AI记忆架构",              // 最近语境
  "energy": "high",                   // 情绪/推进欲
  "interaction_mode": "高频技术对话+情绪反馈",
  "pending_question": "如何解决连续人格",  // ★一等字段：上次“正要决定”的开放线，最触发“接上了”
  "updated_at": "..."
}
```
字段要**模型无关**（结构化），配一套明确注入模板，保证 Claude / DeepSeek 都能凭同一张卡复现“同一个人”。

## 四、运行机制
1. **更新（状态驱动，非记忆驱动）**：每轮（或每 N 轮）结束，一次轻量 LLM 调用把“这一轮”**增量折叠**进状态卡（不是重算）。
2. **新对话启动**：不塞历史对话，而是**加载状态快照 + 重建上下文**——注入三层 + 环境层 + 续接上下文。目标 **<300 token**（不含续接），效果像连续对话。
3. **压缩**：状态卡定长小 JSON，天然低 token；事实层用【记忆簇】的合并管线控量。

行为对比：
| 模式 | AI 行为 |
|---|---|
| 记忆驱动（旧） | “你做过 Arix” |
| 状态驱动（新） | “我们现在在深度设计系统，语气偏工程推进” |

## 五、粒度决策（待最终确认）
- **状态卡挂在角色卡层级**（而非单会话），才能跨新对话延续 → 对症 bug「开新对话他还是他」。多用户暂无 → 先每角色一张。
- **bug「抽取归到通用而非角色卡」= 事实层归属错**：抽取时按当前会话的 `characterCardId` 落库；另做「未定义记忆（未分配角色卡）→ 归档=分配一个角色卡」的补救流程（见 TODO 记忆簇）。

## 六、召回栈总览（各层拼起来）
```
长期事实(记忆) + 人格(角色卡) + 关系(状态卡)      ← 慢，几乎不猜
  + 会话摘要 + 最后 3~5 轮（按时间闸门带）          ← 中，续接“刚才在干嘛”
  + 环境/传感（时间·电量·健康）                     ← 快，现测不是存
  + 当前输入                                        ← 只此需纯推断（那 10%）
```

## 七、环境/传感上下文层（新增 · 手表专属优势）
把“当前状态”从“猜”变“测”——手机/桌面 AI 做不到的差异点。
- **每条消息注入现测信号**，不是加载存储；极低 token；以系统上下文行形式挂在该轮前，非用户消息，如 `[当前 2026-07-07 23:14 · 电量 18%]`。
- **Level 1（P1，先做，零/低权限）**：**时间（已定：所有消息都带）** + 电量（`BatteryManager`）。解锁体贴行为：深夜放缓、低电量精简/提醒充电——也是“AI 主动关心”的地基。
- **Level 2（P2，需权限+接口）**：健康信号——心率/步数（`SensorManager` `TYPE_HEART_RATE`/`STEP_COUNTER`，`BODY_SENSORS` 权限）、睡眠（Health Connect / 厂商 SDK，按本机确认）。**开关 + 授权**，只读最近缓存值，不轮询（省电）。
- **铁律**：传感器是**信号非真相**，测的是**唤醒度**（累不累/激动不激动）非**情绪好坏**（开心不开心）；标为推测，让模型现场校准（例：高心率≠生气，可能运动/咖啡）。

## 八、续接上下文（快变量）
- 组成：**会话摘要 + 最后 3~5 轮** + **时间闸门**：几分钟内→带原文+摘要；几小时~几天→只摘要；更久→丢，只剩慢变量（事实+人格+状态卡）。
- **摘要生成 = 方案 B 滚动摘要**（每 N 轮更新，抗手机不干净结束）。一次 LLM 调用同出「状态卡增量 + 摘要 + pending_question」，可挂**便宜/免费模型**（接 TODO「每功能模型绑定 + 免费 api」）。
- **与「对话归档（总结摘要）」复用**：归档时产出的摘要 = 续接用的摘要，一份两用。
- 工程约束：不塞几十条原消息，最后 3~5 轮 + 几十~几百 token 摘要即可。

## 九、注入总原则（防“猜错还很自信”）
所有加载/推测内容一律标「**上次快照/推测**」，非当前真相。注入模板：
> “以下是上次结束时的状态推测与设备实时信号，仅作先验/参考。若本次开场与之不符，以本次为准。低电量可精简，深夜可放缓。”
状态给得越多越要给模型“随时推翻”的权限，否则连续感会变僵硬感。

## 十、收益
一次做对：修 bug（记忆归属、人格延续）+ 支撑记忆簇 + token 优化（三层+环境 <300 token 取代塞历史）+ 手表体贴行为。
**做完后，项目从“聊天机器人”升级为“有连续人格的系统”。**
