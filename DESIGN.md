# Arix 前端重写设计系统（MD3 → M3E）

> 本文件是前端整体重写的**唯一设计基准**。你、AI 工具（Claude/DeepSeek）、任何一轮都照此执行，
> 避免各画各的。改设计先改本文件再动代码。参考：Material 3 官方 https://m3.material.io/
> 更新：2026-07-02（方向确认轮）

## 0. 目标与约束
- **设备**：**方屏智能手表为主**，手机也要能用 → **手表优先 + 响应式**。
  - 大点击区（≥48dp）、少层级、满屏视图、避免密集小控件与超小字号。
  - 用标准 Compose Material3（非 Wear Compose）；手机上自适应放大留白。
- **气质**：排版像 **ChatGPT 客户端**（单列聊天流为主）；整体**像 RikkaHub 一样简洁干净**；
  但**保留 Arix 上手即用、不用研究就会配置**的优点（避开 Operit 的臃肿与配置门槛）。
- **只换皮不换骨**：重写仅动表现层（Compose UI）。业务逻辑（managers/tools/DB/网络）保持不动。
- **全程可跑**：一页一页重写，替换进现有导航；每页编译+测试+提交。

## 1. 主题引擎（运行时深度可定制 — 不是编译期写死）
用户可**运行时自选**：字体、按钮/组件样式、配色、背景等。所以主题是一套**持久化的
`ThemeConfig` 状态**（存 DB/prefs）→ 驱动 `MaterialExpressiveTheme`，改设置即时生效。

- **配色（多来源莫奈取色）**：
  - 默认 **手机壁纸** 莫奈动态色：`dynamicLightColorScheme(ctx)` / `dynamicDarkColorScheme(ctx)`（API31+）。
  - **任意图片提色**（如**聊天壁纸**）：用 material-color-utilities 从 Bitmap 提种子色 → 生成整套 M3 配色。
  - **用户自定义主色**：`colorpicker-compose` 选色 → 种子色生成配色覆盖。
  - **回退**：API<31 或不可用 → 内置品牌配色。亮/暗都支持。
- **字体**：用户可选字体族（内置若干 + 预留用户自备/下载）。走 `Typography` 令牌。
- **按钮/组件样式**：用户可选风格变体（填充/描边/色调、圆角大小、形状）→ 由 `ThemeConfig` 映射到 `shapes` 与组件默认样式。
- **背景**：应用背景 / 聊天壁纸可自定义图片（并可作为上面的提色来源）。
- **UI 风格 / 主题包**：把上面全部外观选择（配色+字体+组件样式+形状+动效+模糊+背景）打包成一个
  **命名的"风格"**，用户可保存/切换多套、内置若干预设。`ThemeConfig` 即这套风格的**可序列化数据模型**。
- **主题包可导入导出/分享**：一套 UI 风格可**导出成文件**（配置 JSON + 引用的图片/字体资源打包），
  也能**导入别人的**，像换主题那样一键套用（详见 §5）。未来可接一个"主题市场"（复用现有云市场思路），非本轮范围。
- **铁律**：所有颜色/字体/形状走 `MaterialTheme.*` 令牌（令牌值来自 `ThemeConfig`），**禁止写死 0xFF 十六进制**。

## 2. 字体 / 形状
- M3E `Typography` 比例；手表上适当放大正文与触控区。
- M3E 表现力形状（较大圆角）；聊天气泡、卡片、按钮统一取自 `MaterialTheme.shapes`。

## 3. 动效
- 用 M3E 组件，但**动效调成快速线性 tween**（不用弹簧物理），短、可打断、流畅省电——贴合"Telegram 丝滑"。
- 集中定义动效令牌（时长/缓动），全局复用；进出/展开/列表项/流式光标/"思考中"都要有动效。
- "思考中"用轻量线性 loading 动效（非重物理）。

## 4. 模糊（性能取舍：已定）
- **默认只在关键面模糊**：顶栏 / 底部输入条 / 弹窗 / 底部抽拉表 / 抽屉遮罩。
- **设置里加"高质量模糊"开关**，默认关；开启后扩大模糊范围。
- **弱设备自动降级**（关闭或降透明度）。方屏手表 GPU 弱，模糊是大户，绝不无脑铺满。
- 技术：`Modifier.blur()`/RenderEffect（API31+，设备是 14/15 OK）；如需背景实时模糊评估 Haze 库。

## 5. 导航 / 信息架构（手表友好）
- **聊天 = 满屏首页**（ChatGPT 式单列 + 底部输入）。
- 设置/管理收进**满屏分组列表**（小方屏上比 14 项平铺抽屉好用）：
  - 主要：对话、对话管理
  - 配置：模型配置、语音识别、语音唤醒、角色卡、记忆管理、权限、应用设置
  - 扩展：本地包、云端市场、插件制作
  - 开发者（收进"开发者模式"开关后，默认隐藏）：终端、监控、触屏控制、崩溃报告
- **导入/导出不再单独成页**：删除 ImportExportPage，改为在各功能页内提供"导入/导出"动作
  （记忆、角色卡、模型配置、本地包等，点开即可就地导入/导出该功能内容）。
- **外观/主题也可导入导出**：外观设置页提供"导出当前 UI 风格 / 导入主题包"，把整套 UI 风格
  （见 §1 主题包）当文件分享与套用。

## 6. AI 输出
- **流式 / 直出都支持**：设置里加开关（默认流式）。
- 流式期间用纯文本渲染，结束后再 Markdown（已在性能治理轮实现，保留）。

## 7. 架构（趁重写一并做）
- **界面与状态分离**：每页引入 ViewModel（已有 `lifecycle-viewmodel-compose` 依赖），
  状态从 `remember` 满天飞收进 ViewModel，Composable 变薄只管画。
- 逐页迁移，不一次性全改；哪页重写就顺手给哪页建 ViewModel。

## 8. 执行顺序
1. **设计系统骨架**：MaterialExpressiveTheme + 配色（动态/自定义/回退）+ 字体 + 形状 + 动效令牌 + 模糊工具。
2. **核心组件**：按钮/卡片/输入框/聊天气泡/加载与"思考中"动效。
3. **样板页**：先重写「聊天」（首页、最高频、ChatGPT 式）当样板，确认风格。
4. **逐页推进**：其余页面按 §5 分组依次重写；同时把该页状态抽到 ViewModel、把导入/导出内嵌。
5. **打磨**：亮/暗、动态色、模糊开关、开发者模式、手表/手机响应式微调。

## 9. 插件系统（用户用 AI 造前端+后端插件）—— 待确认，独立工作流
用户能用 AI 生成插件包，自己实现 Arix 没有的功能（类 Operit 插件），**插件同时贡献前端 UI + 后端逻辑**。

**现状（重要）**：目前**没有可用的插件执行引擎**。
- `PluginManager` 走 dex/jar 加载 Kotlin 插件 → AI 无法在设备上现编译 dex，不适合"用户 AI 造"。
- `OperitCompat` 能解析 Operit `main.js` 包，但 JS 执行是**占位未实现**（需 WebView 引擎）。

**机制决策：待深入调研后定**（用户要求）。启动此工作流时先调研 Operit 插件实现 + 对比
WebView+JS / Compose-DSL / 其它方案的利弊，再定。调研放到工作流启动时做（现在做会过时）。

**当前倾向（调研起点，非最终）**：**WebView 承载的 JS/HTML 插件**
- 后端：插件 JS 通过 **JS↔原生桥** 调用 Arix 的 `ToolManager`/设备/HTTP 等能力。
- 前端：插件 UI = HTML/CSS/JS 在 **WebView** 中渲染；把当前主题色以 CSS 变量注入，使插件界面与 App 主题一致。
- 集成：插件可在导航中出现一个入口；插件 UI 挂载到核心 UI 预留的"插件宿主"位。
- 安全：复用现有 `ToolPermissionManager` 权限闸门管控插件对工具/设备的调用；WebView 沙盒隔离。
- 理由：AI 最擅长生成 HTML/JS；可沙盒；对得上 OperitCompat 已留的 `main.js` 占位方向。

**沙盒手表小屏**：插件 UI 需响应式，适配方屏。

**排期建议**：这是**独立的大工作流**，**放在核心前端重写之后**做。核心重写时只需**预留插件宿主的接缝**
（一个能挂载 WebView 插件页的宿主 + 导航入口），不要在重写核心 UI 的同时去建整个插件运行时（范围失控）。

## 10. 不挖坑铁律
- 颜色/字体/形状/动效**只用令牌**，不写死。
- 一页一提交，编译+测试过才算完。
- 同一批文件同时只让一个 AI 动。
- 只动 UI，忍住别重构业务逻辑（范围失控是新坑之源）。
