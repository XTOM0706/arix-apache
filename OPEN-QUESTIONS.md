# 存疑清单 · Arix

> **这份文件专收「拍脑袋定的数」「没验证的判断」「明知有缺口但没做的事」。**
> 记在这儿是为了别把它们当成设计。每条都写清**怎么才算判定了**——不要凭感觉调。
>
> 状态口径：`待验`=要真机/用一阵子才知道 ｜ `待你定`=产品判断不是技术判断 ｜
> `已知缺口`=明知有问题本轮没做 ｜ `已知限制`=做不到或不值得做 ｜ `已修`=后来处理掉了
>
> 起始：2026-07-27（P1 工具调用层 + P2 理解层）。新增条目往后追加，别改旧编号。

---

## P1 · 工具调用层（2026-07-27）

| # | 现在是什么 | 疑点 | 怎么判定 | 状态 |
|---|---|---|---|---|
| Q1 | 旧工具结果裁剪：整段对话 **<16000 字符不裁** | 门槛可能太低（手表上常见对话根本到不了）或太高（长上下文模型没必要这么早裁） | 长对话跑几轮，看第一次触发裁剪时对话有多长、模型有没有因此丢信息 | 待验 |
| Q2 | 附件分档线 **2500 / 9000 token** | 应该跟着当前模型的上下文窗口走，而不是写死 | 同一份中等文件在 8k 窗口和 128k 窗口的模型上各跑一次 | 待验 |
| Q3 | 熔断：同调用 **3 次警告/5 次断**、同工具 **不再警告/12 次断**、最近 4 步序列 **2/3** | 提醒已按 Q10 放宽并改成只给模型看；剩下的疑点是「12 次硬停」会不会掐在正经长任务中间 | 正常用几天，看有没有被「原地打转」掐掉本该继续的活 | 待验（2026-07-28 按 Q10 调过一轮） |
| Q4 | 角色卡「工具范围」塞在 **400dp 高的编辑弹窗**里，明细是 50 来个包的勾选列表 | 小圆屏上大概率点不动/看不全，可能得改成独立页面 | 手表真机打开编辑器翻到那一节试着勾几个 | 待验 |
| Q5 | 工具结果落盘后指向 `read_document` | 用户关了 doc_read 包、或角色卡把它排除，路径就是死的 | —— | **已修**（现在先探五条判据，不可达就头尾预算翻倍多内联） |
| Q6 | 能力探测 **30s TTL**，探测里有 4 次 binder 调用，发生在工具循环所在线程 | 手表上是否有可感的卡顿；TTL 30s 是否让「刚开完权限」等太久 | 开着高频工具循环看掉帧；开完无障碍后数几秒工具表才认 | 待验 |
| Q7 | 图片：入站 **>1.2MB 才压**、边长 1600、质量 85→45；历史图**只留最近 2 个带图回合** | 压得够不够狠、留 2 轮够不够用（追问上上张图就没了） | 连发几张大图聊几轮，看请求体积和追问旧图的效果 | 待验 |
| Q8 | token 估算口径 **中文 1.2 / 其余 4 字符每 token** | 对 JSON、base64、代码这类偏差可能很大（分档会判错） | 拿真实 usage 回报的 `prompt_tokens` 对一次账 | 待验 |
| Q9 | 附件读取上限 **600KB 字符**，超过只走「卡片」档、行数写成 `≥N` | 超大文件的行数与 token 都是低估的，模型可能低估工作量 | 丢个 50MB 的 log 看卡片说得对不对 | 待验 |
| Q10 | 打转提醒 ⚠ 会**显示在用户可见的工具卡**里 | 是透明还是吵人 | 你自己看着烦不烦 | **已定**（2026-07-28 用户定：「反复调用一个工具很正常，人干活也这样，搜不到就会反复搜」）→ 提醒只拼进给模型的那条 tool 消息，用户可见的工具卡是干净结果；删掉「同工具调了 N 次」那条提醒；同名同参从第 2 次改第 3 次才提醒；硬停同工具 8→12 次 |
| Q11 | 角色卡导出 JSON 不含工具范围等 per-card 旁路数据 | 换机/分享会丢 | —— | **已修**（补了 worldBook/工具范围/对话示例/替换规则，导入侧兼容老文件） |
| Q12 | 新增 UI 串走了 `tr()` 但没跑 i18n 管线 | 英文缺 | —— | **已修**（英文 12 条补齐；其余 32 语仍空，等 opencode 批量译） |

---

## P2 · 理解层（2026-07-27）

| # | 现在是什么 | 疑点 | 怎么判定 | 状态 |
|---|---|---|---|---|
| Q13 | 记忆注入在**发送主路径上**，含一次 embedding 网络往返，加了 **1500ms 超时** | 超时就这轮不注入、而且是静默的——用户只会觉得"它这次怎么忘了"。手表弱网下可能常态化触发 | 弱网/关掉 embedding 端点各发几条，看首字延迟与注入是否稳定 | 待验 |
| Q14 | 常驻块预算 **1200 字符**、单条截 **160 字**、置顶最多占 **40%** | 三个数都是拍的；160 字可能把长记忆截得没用 | 真机看注入块长什么样、模型是否真用上了 | 待验 |
| Q15 | 检索打分：`max(kw, 0.3kw+0.7vec)`、同卡 ×1.25、置顶 +10、邻居两跳衰减 0.6 上限 8 | 全部无数据支撑；置顶 +10 让置顶恒排前，置顶多了会挤掉"这次相关的" | 造一批记忆做几组检索，看排序合不合直觉 | 待验 |
| Q16 | 教训沉淀**默认开、没有开关**；重复发生 importance +0.1 封顶 0.85；`not_found` 不记 | 会不会攒出一堆噪音教训挤占检索；用户可能不想要自动写入（虽然记忆页可见可删） | 用几天看记忆页里 `教训·` 开头的条目质量 | **已定**（2026-07-28 用户定：教训沉淀保持默认开，另加"自动整理记忆"评估教训有没有用；拿不准的问用户） |
| Q17 | `assertedAt` 建了列但**没有任何代码写它**，检索也没用它 | 现在是个死字段。时效/冲突消解（新事实推翻旧事实）没做 | —— | **已修**（2026-07-28，不动 schema，列本来就有）<br>**① 写它**：规则集中在 `MemoryManager`，`assertedAtOf()` 统一读（0=未知→回退 createdAt，老数据行为不变）。刷新的路径 = `FRESH_SOURCES`（ai_tool/auto_extract/lesson/hard_signal/user_input）**且内容真的变了**：`add` 新建、`upsertByTitle` 同标题命中后换了内容、语义去重命中后换成新说法（同一件事又被说一遍=此刻仍成立）、`memory` 工具的 update。不刷新的：记忆页手动编辑（订正≠改口，`update(reassert=false)` 是默认值）、只改重要度/标签、导入/备份恢复/回收站还原/AI 合并（还原的是历史断言）。<br>**② 检索用它**：`rankRelevant` 里只有 `event`/`todo` 两类按断言时间衰减（半衰 30 天、最多吃掉三成分数）；其余六类一分不减——偏好/长期事实靠冲突消解换代，不靠放久了自己掉下去。所有类别再加一个 `+0.02×新鲜度` 的**平局裁决**，让同类同分时新断言的排前面。<br>**③ 冲突消解**：写入时那一次向量比对顺手多取一段「同题不同话」的旧条目（余弦 0.80~0.92），后台叫一次便宜模型判「新的成立后旧的还成不成立」，判 true 的**归档不删**：打一条 `superseded_by` 边（`relatedMeta` 里，反向 `supersedes`）+ 重要度 ×0.5。归档的不再进检索/注入/邻居展开，但记忆页照常列出来、带「已被更新的说法取代」标记和「恢复」按钮（`unarchiveSuperseded`）。判不出来（没配 embedding/没配模型/解析失败/模型说不确定）一律原样不动。 |
| Q18 | `relevantPack()` 新 API **目前没人调用**（注入走 `MemoryInjection` 自己拼） | 两套拼装逻辑并存，迟早不一致 | 二选一：注入改走 relevantPack，或删掉它 | **已修**（2026-07-28 删掉 `relevantPack`：检索本就同走 `rankRelevant`，只是排版各写一版；留活的那份，它预算规则更全） |
| Q19 | 跨卡同名记忆现在会**新建**（修归属 bug 的必然结果） | `GitHubBackup` 注释里"靠标题合并所以反复恢复不会翻倍"的保证，现在只在**卡内**成立 | 反复恢复同一份备份，数一数条数 | 待验 |
| Q20 | 压缩护栏的比值分母用 `body`（含上一版摘要） | 新增段落小、旧摘要大时会判成"没压下来"而跳过，行为上是延后一轮 | 长对话跑到第二、三次压缩看还压不压得动 | 待验（设计选择） |
| Q21 | 删对话时**不删** `archive/` 里的原文存档（每对话最多 6 个文件） | 隐私与空间：会话删了，原文还躺在工作区 | —— | **已修**（2026-07-28 删除收口到 `ConversationManager.delete/deleteMany`：删库行后调 `ContextCompressor.purgeConversation` 清掉该对话的原文存档与摘要缓存；四条删除路径全改过来——列表菜单、列表批量删、抽屉长按、`manage_chats(action=delete)`。存档与对话的对应关系＝文件名前缀 `conv<id>_`，由新抽的 `archivePrefix()` 三处共用；判据保守：只删该前缀 + `.txt` 的普通文件、且 canonical 父目录仍是 `archive/`，其余一律留着） |
| Q22 | 存档只存消息 `content`，不含工具调用参数/图片 | 检索回来的历史是不完整的 | —— | 已知限制 |
| Q23 | 硬信号里「近期日程」判空靠 `content.startsWith("未来")` 字符串耦合 | `CalendarTool` 改一句文案就静默失效 | 改成结构化返回或加标记 | **已修**（2026-07-28 抽出 `CalendarTool.upcoming(days)`：null=没权限、空表=真没日程，硬信号拿结构不猜文案） |
| Q24 | 硬信号的「身体与作息」存的是**快照**（今日步数/当前心率），不是规律 | 名为长期记忆，实为一次采样，隔天就过时 | 需要 `HealthSignals` 暴露睡眠等聚合值才能改对 | 已知限制 |
| Q25 | 硬信号的「常来消息的应用」取自进程内环形缓存 | 服务刚重连时基本取不到，实际命中率未知 | 用几天看这条记忆有没有被写出来 | 待验 |
| Q26 | 角色卡导出新增键名 `toolExcludes`/`roleplayExamples`/`displayRules` | 会成为对外格式的一部分，要改现在最便宜 | 你定 | **已定**（2026-07-28 用户定：导出只导角色卡本体，不连带捆绑；这三个键全部从卡文件里删掉，改走各自的独立导出入口）<br>做法：判据=「字段长在 `CharacterCardEntity` 上吗」。`toolExcludes`(`CardToolStore`)、`roleplayExamples`/`displayRules`(`CardRoleplayStore`) 是按 cardId 存在 SharedPreferences 的旁路数据 → **移出**。`exportCharacterCard` 去掉 `context` 参数；导入侧删掉 `restoreCardSideData`，老文件里带这三个键**只是被忽略**（不崩、也不再让外来卡改本机工具范围）。旁路数据在「导入导出中心」新增两个独立入口：`exportCardToolScopes`/`importCardToolScopes`（角色卡工具范围）与 `exportCardRoleplay`/`importCardRoleplay`（角色卡扮演增强），按**卡名**对回本机同名卡、找不到就跳过并报数。<br>⭐ 追加（同日，用户："世界书本来就不应该跟着角色卡，而且不是必须的"）：`worldBook` 虽然是卡实体的一列，**也一并移出卡文件**——导出不带、导入不往里写。外来卡内嵌的 lorebook（酒馆 `character_book` / 我们老版本的 `worldBook` 键）不丢也不塞进卡：落成一条**独立世界书**再绑到新卡上（`WorldTreeStore.save` + `bind`），于是它是个看得见、能单独改、能解绑的东西。卡上那一列保留只为兼容早先建的卡。世界书本就有独立入口（世界树页 + 粘贴导入的「世界书」类型）；换机整搬仍由全量备份覆盖（`FullBackup` 打包整个 `shared_prefs`）。 |
| Q27 | `MemorySalvage.extractMemories`（救援路径）的提示词仍是 5 类 | 救援抽不出 lesson/environment/convention | 改那句提示词即可 | **已修**（2026-07-28 抽取范围从「只抽关于用户的」扩成「也抽关于怎么干活的（踩过的坑/这台设备的情况/用户定下的规矩）」，type 枚举与 `MemoryTool`、`llmMerge` 三处一字不差：preference/fact/event/relation/todo/lesson/environment/convention） |
| Q28 | 改了两处**提示词**：`memory` 工具描述扩成"也记怎么干活的知识"、`llmMerge` 的 type 枚举补 8 类 | CLAUDE.md 要求提示词改动要过你的眼 | 你审 | **已过目**（2026-07-28 用户："可以"） |
| Q29 | Room 迁移 **19→20** 编译通过，但没在装着旧版的真机上升级过 | 迁移路径只在真机升级时才走到 | 用旧版 APK 装一次、聊两句、再装新包 | 待验（**风险最高**） |

---

## P0 / P3 / P4 收尾（2026-07-27）

| # | 现在是什么 | 疑点 | 怎么判定 | 状态 |
|---|---|---|---|---|
| Q30 | **R8 release 构建通过**（53.6MB），但只是"编译链路通了" | R8 真正的炸点是**运行时**：反射/JNI/序列化被剪掉要跑起来才知道。keep 规则是我逐条推的，不是实测出来的 | 装 release 包，把主要路径都走一遍（唤醒、终端、PDF、Shizuku、图表/公式渲染、备份恢复） | 待验（**风险最高**） |
| Q31 | release APK 里 **BouncyCastle 的后量子算法资源占 7.79MB**（实测：lowmc + sike p751/p610/p503） | 我们不用后量子加密，纯白带。PDFBox 拉进来的 | `packaging { resources { excludes += "org/bouncycastle/pqc/**" } }` 之后跑一遍 PDF 解析确认没炸 | **已做**（2026-07-28 排掉，release **53.60MB → 47.18MB，省 6.42MB**，APK 里 pqc 资源归零）。**待验**：PDF 解析（含加密 PDF）跑一遍确认没炸——排的是 pqc 资源、不是 BC 本身，PDF 走的 RSA/AES 那条路不该受影响，但这是判断不是实测 |
| Q32 | `shrinkResources` **没开** | 本项目有大量按名字取的 raw/assets（模型、脚本、字体），开了容易静默丢资源 | 要开就得先把按名取资源的地方列全 | 有意不做 |
| Q33 | HTTP MCP 工具**强制加 `mcp_` 前缀**（安全修复的副作用） | 现存 HTTP MCP 工具会改名，用户对旧名设过的 per-tool 策略回落默认 | 你决定接受还是回滚这一处 | **已定**（2026-07-28 用户："直接改，现在的 user 只有内部测试人员，不用管数据"）→ 保持强制前缀，不做任何策略迁移 |
| Q34 | 模型自动审批**不再对插件/MCP 生效** | 开了自动审批的用户会发现插件/MCP 多弹框了 | 你决定接受还是回滚 | **已定**（2026-07-28 用户："接受，然后增加提示第三方 MCP 不支持自动审批"）→ 权限页开关说明加了一句；授权弹窗在第三方发起时多一行「（第三方调用不走自动审批）」，悬浮窗版共用同一份内容 |
| Q35 | MCP 客户端身份 = **远端 IP:端口**，源端口是客户端自选的 | 用户对某次连接点过"始终允许"后，本机任一 App 只要 bind 同一源端口再连过来就继承那条授权 | 修法：对 `ToolCaller.Mcp` 不提供"始终允许"，或 clientId 改用 auth token 指纹 | **已修**（2026-07-28 两条都做：设了令牌 → clientId=令牌指纹且 `verified=true`；没设 → `canRememberAlways=false`，弹窗不给「始终」按钮、`resolve` 也不落盘） |
| Q36 | 市场包 / script 包声明的工具名同样能**覆盖内置工具** | 与 Q33 同源，只堵了 MCP 这一条 | `ToolManager.register` 对内置名拒绝覆盖或自动加包前缀 | **已修**（2026-07-28 拆成 `registerBuiltin`（内置，名字进保留表）/ `register`（第三方，撞名改注册成 `ext_<name>` 并返回实际注册名）；市场包/插件/技能三条反注册都改按实际名） |
| Q37 | MCP 连接复用是**读代码得出的结论，不是实测** | 竞品 7 条投诉都在这块，我们判"我们没这问题" | 配一个会打印 pid 的 shell MCP server，连调两次看 pid 是否相同 + 数进程条数 | 待验 |
| Q38 | stdio MCP 写失败时**重发一次** | 前提是"write 返回 false ⇒ 字节一个都没出门"。sh 兜底那条若在 flush 中途抛异常，理论上可能已写出半行 | 半行本身就是坏 JSON、对端也解不出来，判断无害——但这是判断不是实测 | 待验 |
| Q39 | `thinking_blocks` 这个键名来自代理层(LiteLLM)实现，**不是某家官方规范** | 猜错键名的代价是白存一份没人认的东西（不会 400，因为只回发同端点自己吐的键） | 抓一次经 OpenRouter 的 Anthropic 思考模型的响应 | 待验 |
| Q40 | 思考签名的 **SSE 分片归并规则是自定的**（按 index 归并，文本类拼接、标量类后到覆盖） | 如果某家把 base64 签名也切片下发，"后到覆盖"会丢前半段 | 真实抓包一次 | 待验 |
| Q41 | 经 OpenRouter 时思考**正文**走 `delta.reasoning`（字符串），我们只读 `reasoning_content` | 侧栏思考文本大概率是空的（显示问题，不是回传问题） | 用 OpenRouter 的推理模型聊一句看侧栏 | **已修**（2026-07-28 `parseDelta` 补读 `delta.reasoning`，只取字符串形态——发成对象/数组的是回传凭证，仍归 ReasoningPassthrough；两个键都有以 `reasoning_content` 为准）。**待真机验**：拿 OpenRouter 推理模型聊一句看侧栏有没有字 |
| Q42 | 网关阈值 **8 个第三方工具**才启用，且**只按数量不按 token 量** | 一个 schema 极臃肿的工具可能比五个小工具还贵，按数量判会放过它 | 装几个真 MCP server 看实际 token | 待验 |
| Q43 | 两个 MCP server 暴露**同名工具**时后注册的覆盖前一个 | 网关目录里只看得到一个（既有行为，网关层没改） | —— | 已知限制 |
| Q44 | `McpGateway.hiddenToolNames` 每次调用**重扫工具表**、无缓存 | 它在每轮 `getToolsJson` 里被调；工具多时值得加一层随 `OperitCompat.refresh()` 失效的缓存 | 工具上百个时测一次耗时 | **已修**（2026-07-28 加缓存：`enabled` 与 `hiddenToolNames` 合并成一次扫描 `gate()`（原来每轮各扫一遍），失效判据两条：① `ToolManager.version`——工具表新增的版本号，注册/反注册/清空/绑包**任一写入**都 +1，故 `OperitCompat.refresh`、技能开关、插件开关、启动装配全覆盖，不是只盯 refresh 一个入口；② 网关 `mode`（auto/on/off），用户切开关时表没变但结果要变。功能包开关不入判据：第三方 MCP 工具从不 `bindPackage`，包开关改不动这个集合，且 `bindPackage` 本身也会 +1。算的过程出异常时返回「不启用」且**不写缓存**，免得一次偶发失败被钉住） |
| Q45 | `headTail` 的 `cap` **不是硬上界**（cap=1200 实际产出 ~1232 字） | 名字叫 cap，后来人容易当硬上限用 | 改名或改成真硬上界 | **已修**（2026-07-28 改成真硬上界：中间那行省略提示按最长可能长度先从预算里扣掉；唯一例外是 cap 小到连保底头 200/尾 150 都放不下） |
| Q46 | `trimOldToolResults` 在**整段没有 user 消息**时一条都不裁 | 当前对话总有 user 所以不暴露；将来若有纯工具流水线场景，省 token 会静默失效 | —— | 已知边界 |
| Q47 | `softWrap` 会把 **CRLF/CR 规范化成 LF**（只在真触发换行分支时） | 严格说不是"一个字不丢"。工具结果基本是 LF，影响小 | —— | **已修**（2026-07-28 选了「真不动换行符」而不是改注释：改注释要在每个调用方都记住"结果可能被规范化过"，而改实现只动一行——弃用 `lineSequence().joinToString("\n")`，改成正则 `[^\n\r]{width+1,}` 只找超宽段、只往段**内部插入** LF，原有 CRLF/裸 CR 原样留着。单测补了 `CRLF不被改写成LF` / `裸CR不被改写成LF` 两例） |
| Q48 | `:logic` 模块目前**只搬了 2 个文件** | 分层试点只验证了"工程上可行"，记忆算法/检索打分/提示词拼装/上下文压缩这些**真正值得搬的还都在 app 里** | 下一轮按"不 import android.*"逐个搬 | 进行中（2026-07-28 又搬了 `ToolArgValidator` 与 `softWrap`→`TextWrap`，:logic 现 4 个文件、单测 4 个类）。⭐ 两条硬约束已探明：① `cloudapi`/`data` 都是 **Android library**，所以凡是碰 `ChatMessage`/`MemoryEntity` 的（上下文压缩、记忆打分）**现在搬不动**，要搬得先抽 POJO；② 用 `org.json` 的代码可以搬，但 `:logic` 里 org.json 必须 `compileOnly`（Android 平台自带，写 implementation 会传递进 APK 撞车），且**编译期那份 AOSP json 的 `keys()` 返回裸 `Iterator`**，`next()` 要显式 `as? String` |
| Q49 | `:tools` / `:mcp` / `:persona` 三个模块是**空的 Android library 壳**（没有任何 .kt） | 早先重构计划的残留，白占构建时间 | 删掉或填实 | **已修**（2026-07-28 删掉三个模块 + `settings.gradle.kts` 的 include。⚠ `tools/` 目录本身留着——里面是 token_cost.py / i18n_*.py / competitor_watch.py，删的只是它的 gradle 模块身份） |

---

## 自动整理记忆 · 教训评估（2026-07-28）

| # | 现在是什么 | 疑点 | 怎么判定 | 状态 |
|---|---|---|---|---|
| Q50 | 限频：常规 **72 小时**一轮；教训攒到 **30 条**提速到 **20 小时**。周期任务本身排 12 小时敲一次门 | 三个数全是拍的。72 小时可能太懒（噪音教训在两轮之间就已经把检索名额吃掉了），30 条这个"密集踩坑"阈值也没有依据 | 用一阵子，看记忆页里 `教训·` 条目的总数曲线：如果长期贴着上限爬就是跑太慢，如果每次跑都判不出东西就是跑太勤 | 待验 |
| Q51 | 冷却期 **30 天**：新教训至少放 30 天才允许被判"没用"；判过"有用"的 **60 天**内不再重判 | 30 天在手表上可能太长（一条从没命中的教训白占一个月检索名额），也可能太短（有些工具本来就一季度才用一次，30 天没命中说明不了什么） | 看被删掉的那些条目里有没有"其实还用得上"的——回收站里点过"恢复"的比例就是误删率 | 待验 |
| Q52 | "被检索命中过"判据 = `lastAccessedAt - createdAt > 60 秒` | `MemoryManager.add` 建条时就把 `lastAccessedAt` 写成 now，所以只能靠差值反推。60 秒是拍的；更麻烦的是**只有 `queryRelevant` 会更新 `lastAccessedAt`**，走别的检索路径（`search`/`searchTop`/记忆页搜索）命中不算数 | 手动搜一条教训再看它会不会被判"从没被想起过" | 待验（**判据可能偏严 → 偏向多删**） |
| Q53 | "重复沉淀过"判据 = `importance >= 0.64`，反推自 `LessonRecorder` 的首写 0.55、每重复 +0.1 | 这是**靠数字反推行为**，跟 LessonRecorder 的常量强耦合。那边一改 0.45/0.1/0.85 任何一个，这里的判据就静默失真 | 改 LessonRecorder 的重要度常量时必须同步改 `MemoryTidy.REPEAT_IMPORTANCE` / `repeatTimes` | 已知耦合 |
| Q54 | "工具已不存在"判据 = 从标题 `教训·<工具>·<类别>` 解析工具名后查 `ToolManager`，`mcp_`/`ext_` 前缀跳过 | 工具表是运行期装配的。周期任务在后台跑时若某个功能包的工具还没注册，会把活着的教训判成死的 | 后台跑过一轮后翻回收站，看有没有"对应的工具已不存在"但工具其实还在的条目 | 待验（**误判即删，风险最高的一条**） |
| Q55 | 一轮最多送模型判 **20 条**；待确认列表上限 **20**；回收站保留 **50** 条 | 待确认攒到 20 条用户多半整批忽略（等于没问）；回收站 50 条按 SharedPreferences 存全文，正文各 500 字 = 最大 ~25KB 单值 | 看用户实际会不会去点那个横幅 | 待验 |
| Q56 | "问用户"走**记忆页的待确认列表**，不走 `SettingProposalBus`（对话内申请卡） | 选记忆页的理由写在 `MemoryTidy` 头注释里（白名单是安全边界／pending 不持久化／插进当前对话是打断）。但代价是：用户不开记忆页就永远看不到，被问住的条目会无限期留着 | 用户如果反馈"根本没注意到有东西要确认"，就得改成对话内提醒 | **待你定** |
| Q57 | 周期任务从 `XtomApp.onCreate` 的后台线程排上（`ExistingPeriodicWorkPolicy.KEEP`），记忆页也补排一次 | 这是本轮**唯一动到的记忆无关文件**（XtomApp.kt 一行）。若与别的改动冲突，删掉那行不影响功能——用户开过一次记忆页就会补排上 | —— | 已知 |

---

## 记忆时效 · 冲突消解（2026-07-28，Q17）

| # | 现在是什么 | 疑点 | 怎么判定 | 状态 |
|---|---|---|---|---|
| Q58 | 冲突消解的候选带 = 余弦 **0.80 ~ 0.92**（下界新拍，上界沿用去重阈值），一次最多拿 **3** 条旧条目去问模型 | 0.80 是纯拍的：太低会把「爱喝美式」和「爱喝茶」这种并列事实反复送去判（费钱、且给了模型误判机会），太高则真正的改口（「在 A 公司」→「跳槽去 B 公司」）根本进不了候选、冲突消解等于没跑 | 记忆页翻带「已被更新的说法取代」标记的条目：**误归档**看有没有本来两条都该留的；**漏归档**看有没有明显互相矛盾的两条都活着 | 待验 |
| Q59 | 归档 = 打 `superseded_by` 边 + 重要度 **×0.5**，但**不低于 0.31** | 0.31 这个下限是为了避开 `MemorySalvage.autoCompress` 的「importance<0.3 且陈旧=临时条目」删除区——归档不该变成"两周后静默删除"。这是**靠数字躲另一处的数字**，跟 `TEMP_IMPORTANCE` 强耦合，那边一改 0.3 这里就静默失效。另外降权可能把一条 `lesson` 压到 `MemoryTidy.REPEAT_IMPORTANCE`(0.64) 以下，让本来判「有用」的教训改判成要送模型 | 改 `MemorySalvage.TEMP_IMPORTANCE` 时必须同步看 `MemoryManager.SUPERSEDE_FLOOR` | 已知耦合 |
| Q60 | 时效只作用于 **event / todo** 两类：按断言时间半衰 **30 天**、最多吃掉 **三成**分数；所有类别另加 **+0.02×新鲜度** 的平局裁决 | 三个数全是拍的。「哪些类算会过期」是我按枚举语义判的，不是实测：`environment`（设备/路径/装了什么）其实也会过期，只是它更该靠冲突消解换代而不是靠衰减，这条判断没验证过。0.02 的平局裁决在极端情况下（一堆分数几乎相同的记忆）也可能变成事实上的全局排序键 | 造几条同题不同时间的 event/todo 做检索，看排序合不合直觉；再看 environment 类有没有出现「旧路径压着新路径」 | 待验 |
| Q61 | `memory` 工具的 `update` 一律当作**重新断言**（刷新 assertedAt），记忆页手动编辑一律当作**订正**（不刷新） | 这是个启发式，不是模型说的：赌「模型不会为了改错别字去调 update，它调 update 就是因为情况变了」。赌错的代价是一条旧事实被盖上今天的断言时间，在冲突消解里去推翻真正新的那条。备选是给工具加一个显式参数，但那违反「工具参数要少」 | 看模型实际怎么用 update：如果出现「只改了个措辞就调 update」的日志，就得加参数 | 待验 |
| Q62 | 冲突消解在**写入后的后台协程**里跑，命中候选带才叫一次模型（用 `summary` 用途的配置） | 成本没量过：自动抽取一轮可能写好几条记忆，每条都可能触发一次模型往返。另外整套逻辑**依赖 embedding 模型**——没配向量记忆的用户，冲突消解完全不生效（静默的，用户不会知道） | 开着自动抽取聊一阵，看有没有可感的流量/耗电；没配 embedding 的路径要不要退化成别的判据（比如同标题+同类型直接换代） | 待验 |
| Q63 | 本轮新增/改动的**提示词两处**：`MemorySalvage.extractMemories` 扩到 8 类（Q27）、`MemoryManager.llmSupersededIds` 是全新的冲突判定提示词 | CLAUDE.md 要求提示词改动要过你的眼 | 你审 | **待你定** |
| Q64 | 记忆页手动新建走的是 `add()`（笨原语），**不做去重也不做冲突消解** | 有意的：`add` 同时是导入/备份恢复/回收站还原的落地口，在那里做自动合并/归档就是数据丢失。代价是用户手打的记忆不参与换代 | 如果用户反馈"我自己改的记忆没顶掉旧的"，就把记忆页那一处改走 `upsertByTitle` | 已知边界 |
